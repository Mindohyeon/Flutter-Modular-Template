name: core_test
description: "A new Flutter package project."

environment:
  sdk: ^3.9.0
  flutter: "^3.32.8"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: 2.7.0
  flutter_lints: ^5.0.0